<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :event_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :event_content %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :event_image %>
    <%= form.hidden_field :image_cache %>
  </div>

  <!-- <div class="field">
    <%= form.label :yoggy_id %>
    <%= form.number_field :yoggy_id, id: :event_yoggy_id %>
  </div>
 -->
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
